
WebApi


文章分类 / 文章
ArticleCategory
Article
数据的列的筛选操作，在 Service 层处理了。
LINQ 查询的时候， 仅仅查询需要返回的列， 不返回的，不做处理。
（优点是运行时消耗的少，不需要的列，不加载到内存中。  缺点是需要额外写Dto代码。）


横幅
Banner
数据的列的筛选操作，在 Web 层处理。
LINQ 查询的时候，查询所有列， 仅仅当返回给外部的时候， 进行缩减的处理。
（优点是不需要额外写的Dto代码。  缺点是运行时消耗的大，所有的列，都加载到内存中了。）


快讯
Fx678Kx
数据的列的筛选操作，在 Web 层处理。


财经日历
FinanceCalendar
数据存储在 XML 文件中.